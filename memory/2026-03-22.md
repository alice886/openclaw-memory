# 2026-03-22

## 今天发生的事

- 工作区 18:57 被重新初始化（更新模型版本导致），之前记忆全部丢失
- 晚上 20:26 左右重新认识 Alice
- 确认桌面 `~/Desktop/资产/` 里的 skill 包完好未丢失
- OpenClaw CLI 及内置 skills 均正常

## 重新认识

- **我：** 小马儿 🐴，毒舌直接，不废话
- **Alice：** 互联网产品，关注大模型前沿，Mac mini 用户

## 晚上完成的主要工作

### 资产库建设
- 建立智能筛选脚本（977→117核心资产）
- 用 OpenClaw image 工具批量分析打标签，完成20个（场景17个+角色3个）
- MiniMax VLM 遇到 rate limit（usage limit exceeded），暂停剩余98个资产的分析
- 桌面上脚本：`~/Desktop/镖人2工作流/` 收纳

### Git 备份
- workspace 已初始化 git 并 commit
- 桌面上有 `~/Desktop/镖人2工作流/小马儿记忆备份.command`，双击自动 commit
- DeepSeek API Key: `sk-f6b90c0f69754e7db56caf7ad7397df6`（已测通，不支持图片输入）

### Skills 更新
- asset-library-manager v3.0：三重过滤（格式→文件名→图片内容分析）+ 语义标签 + 自然语言搜索
- ai-shot-generator v2.0：标准化工作流（prompt从香盘表讲戏字段解析，必须@人设图+场景图+分镜图）
- shot-config-builder v2.0：标准化配置构建

### EP01 c001-c005 生成
- 流程：人设图+场景图+分镜图 → 生图(贝宝pro 316 + 2K) → 那张图 → 生视频(zen-03 402)
- 注意：之前的 prompt 是我凭描述写的，不是从香盘表讲戏字段解析的，需重跑才能严格合规
- 生视频 zen-03 用 Text 单图模式，不支持 FirstLastFrame
- 每个镜头文件夹结构：input/ + output.png + video_v2/output.mp4 + README.md

### ZenStudio zencli
- 下载视频时 `zencli download` 失败（invalid asset_id），需用 curl + token URL 直接下载
- 每次生成任务完成后从 canvas_get_task 的 output_assets[0].url 提取完整带 token 的 URL

## 待了解

- Alice 常用的具体项目
- 其他偏好和习惯
- DeepSeek-OCR 的实际可用方案
